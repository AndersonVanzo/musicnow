import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
    padding: 16,
  },
  image: {
    aspectRatio: '1 / 1',
    height: undefined,
    resizeMode: 'cover',
    width: '100%',
  },
});
